import React from 'react';
import './Header.css';

const HeaderContent = () => {
    return (
      <div className="headercontent">Let's learn React by building simple interfaces with components. Don't try o overthink it, just keep it simple and have fun. Once you feel comfortable using compoenents you are well on  your way to mastering React!</div>
    );
  };

export default HeaderContent;